{
    "description": "Example of using testing library using Typescript",
    "author": "Google Inc.",
    "engines": {
        "node": "~6.0"
    },
    "scripts": {
        "build": "npm run lint && tsc",
        "lint": "tslint -p .",
        "clean": "rm -rf dist",
        "test": "npm run build && ava -c 1 -s dist/*.js"
    },
    "devDependencies": {
        "@types/chai": "^4.1.3",
        "@types/winston": "^2.3.9",
        "nyc": "^11.6.0",
        "tslint": "^5.9.1",
        "tslint-eslint-rules": "^5.1.0",
        "typescript": "^2.9.2",
        "ava": "^0.20.0",
        "chai": "3.5.0",
        "chai-spies": "^0.7.1",
        "mocha": "^3.0.2",
        "winston": "2.2.0",
        "actions-on-google-testing": "^0.1.0"
    }
}
